.DS_Store
node_modules/
*.log

# Local + personal only — never publish
.claude/

# Bundled 5etools content stays local (copyright). Generated by extract.py.
data/
# Offline build embeds that data — local only. Public build is docs/ (no data).
dist/
scratchpad/mockups/
