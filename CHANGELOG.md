# Changelog

The version in the app's footer comes from `VERSION`, which `build.py` injects into every
deliverable. **MAJOR.MINOR.PATCH** (D117, 2026-08-28): the **major** moves only for
overhauls or massive reworks, on Francesco's say-so (`python3 bump.py --major`); the
**minor** for larger batches that ship features (`--minor`); the **patch** once per
ordinary commit (`python3 bump.py`). Releases are tagged (`git tag v1.2.2`) from 1.2.2 on.

**The old two-part 1.x line (1.0 → 1.8) is mapped, not rewritten** — same commits, same
SHAs, retro-tagged with their semver identity, exactly as the pre-1.0 line was. The table
below names both. Everything before 1.0.0 is the internal v4 → v7 line, mapped onto a
pre-1.0 series and tagged in place. `git tag` lists them all; `git show v0.6.5` opens one.

## 1.x — the numbered line

| Version | (was) | Commit | What shipped |
|---|---|---|---|
| **1.6.15** | — | — | **A card shows the slot as it stood at that level** (**D207**, his report on the repaired build: *"higher level spells drifted to lower level slots (ex. grave ground at level 1)"*). **Nothing had drifted** — the data was right and the guide card was reading the wrong end of the trade chain. `gpickSec` sliced the RAW array, so a traded slot showed its FINAL occupant on every card, including levels before the trade: his **L1** card drew **Banishment (lv4)** and **Dispel Magic (lv3)** in a slot capped at 1, and **L2** drew **Grave Ground (lv5)** — the L7 and L9 trade-ins. It is also why the chip was never flagged red: `guideSecIll` unswaps, so the flag was judging Arms of Hadar while the chip showed Banishment, and each was individually consistent. **This is what he had been reporting since the first screenshot.** The section now carries both keys the way the timeline always has (D115(j)): `keys` is the unswapped identity at that level and drives the chip, the rail line and the cap; `raw` is what sits there now and drives every writer. A slot a LATER trade owns drops its ✕ and says which level owns it — a ✕ there would hand a writer a spell that is not in the array. Fixtures N1–N5. |
| **1.6.14** | — | — | **A spell you traded away says so, at the level you traded it** (**D206(i)**, off his *Fervent Kuo-Toa* export). His **15 / 10** Warlock 10 is **four** spells his own `swaps` record says he gave up — Arms of Hadar (L3), Battle Familiar (L4), Comprehend Languages (L5), Armor of Agathys (L6) — sitting past the schedule, re-appended after they were correctly removed; plus one genuinely over budget. The trade **chain itself is intact**: positions 0/1/2/4 resolve exactly as D188/D115(g) intend. What was wrong was the sentence — all five read *"is one spell more than Warlock 10 learns"* at **L10**, five levels from anything he did. `buildHealth` now distinguishes the two: a give-up still in the list is named as **a trade that did not finish**, at the level of that trade, with what to do about it. Only off-schedule — a spell given up and later learned again holds a real slot and is an ordinary pick, which the sweep must never accuse. Nothing is removed automatically (D42). Fixtures M1–M4. |
| **1.6.13** | — | — | **A drag is a swap, and a placement asks the same cap** (**D206(h)**, his follow-up: *"does this change affect current builds? I still see the issue even after pushing the change"*). Two true answers. **① The fix is preventive, not curative** — an array already re-dated stays re-dated, since nothing rewrites stored picks (D42); a build damaged before v1.6.12 still reads wrong. **② There were still live paths**, in the two writers that move a pick on purpose and that the audit had exported but never driven. `dropChipOnLevel` fell back to a splice whenever the target level held no empty slot, so dragging the L8 pick onto L5 moved **L5→L6, L6→L7 and L7→L8** — three picks the drag never named. **A drag exchanges two positions now**, which is what its own empty-slot branch already did. `guidePlace`'s drift is kept — D118(g)/D134(a): *the repair is placing a legal pick and letting the offender drift later* — but it asks `slotTakes` before writing, because "never placeable into a low slot" had been settled in the picker's cap alone and the writer would still take one handed to it. Audit now **43 assertions, 12 of which were red**. |
| **1.6.12** | — | — | **Nothing re-dates a pick you did not name** (**D206**, his report: *"the bug of misplaced spells when one is removed keeps occurring, audit it fully and investigate the root cause"*). The audit came first (1.6.11) and **8 of its 37 assertions failed**. **The root cause: a take made while VIEWING a level spliced into the middle of the array** (`toggle`, and `markTake`'s twin) — and the guide sets the preview level on every step you stand on, so that is the ordinary state of a walk. One click re-dated **six** picks, pushed the last off the schedule, and dropped the new pick into a slot it could not legally have been learned in: viewing L1, the insertion point IS the L2 slot, which is the level-3 spell on his L2 Warlock card. D186(a) had removed the pull-back splice from the same block and left this one — its comment said *"Nothing moves now"*. A take that displaces nothing still appends; anything else is **refused and says why**. Three more closed with it: **`slotTakes` is the one owner of the cap test**, so the sectioned take (D184) is checked like every other landing; the trade's give-up carries the **position** it offered instead of a name to look up again — the silent no-op behind his **15 / 10** Warlock 10, and the ⚑ open since 2026-09-05, which never reproduced on a clean build because it needs a later trade to exist; and **undoing one half of a trade no longer evicts the other** (L1 → L8 in the audit). A refusal wears the gold warn notice, not a green check. |
| **1.6.11** | — | — | **A slot audit that holds D146's one invariant** — tooling, ahead of the fix. `scratchpad/slotaudit.js` states the rule as one sentence (*touching one pick never re-dates another*) and drives the REAL writers at every position of a real Warlock 8 to break it: the three drop paths, the sectioned take, both trade halves, the previewed take, `unswap`'s position integrity, the guide's own view, a multiclass row. `scratchpad/sbload.js` is the node harness extracted out of `engine.test.js` so both runners boot one copy. The export shim carries the slot model and every writer that can move a pick between slots, plus `set.spellBy` — `holeFor` asks a spell its LEVEL, so without it the legality check silently no-ops in the harness. Gate line nine. |
| **1.6.10** | — | — | **A card drops the opener for the list in front of it, not for any list** (**D205**, his report: *"here I don't see how to actual set a spell for agonizing blast"*). The step read **1 of 2 answered**, the rail carried *choose one of your known Warlock cantrips that deals damage · skipped, still open*, and the card drew nothing for it. `noOpener` was `inline||chipped` — step-wide — and `inline` means only that SOMETHING is hosted in the stage. The step's other section is an **`optfeat`**, which `guideSecOpen` leaves out of the openers, so clicking **Change it** hosted the invocation picker, `chipped` stayed false for want of a second opener, and the cantrip group lost its button to a list that was never its own — then, with no chips of its own yet, deleted itself on the `!b.children.length` guard. The chip row D165 added to keep a second section reachable never drew for the same reason. `noOpener` is a **per-section predicate** now: a button is redundant beside a chip that opens the same picker, or beside **that section's own** open list, never beside somebody else's. Verified on a real Warlock 1 holding Agonizing Blast with the invocation picker hosted: both sections draw, and **Choose a cantrip** swaps the hosted list to the cantrip pool. |
| **1.6.9** | — | — | **The level-up trade is one surface, closed at rest** (**D204**, his note: *"this screen takes up way too much vertical space, it should be reorganized and compressed"*). On a level where nothing had been traded — which is nearly every level — the trade stood **448px** tall: each kind was a section holding **two more sections**, six frames and four uppercase labels to offer two dashed chips, with `GIVING UP · not yet` sitting directly above a chip that read `− Trade one away`. Four candidates were drawn at the card's real width and measured (`scratchpad/mkswap2.py` → `mockups/swapc.html`); he took the third, amended. Now: **one surface for the whole step**, drawn once over both kinds — no section header, no Optional tag, no note line, the kind an inline row label beside its two halves. **Closed at rest** to a single dashed full-row opener (the score popover's Add, D177/D193, on D198's 34px scale) which opens on any half being set or by hand, and **never re-closes over an answer**. **The spell you gave up is struck through** — its name only, since the level badge and the ✕ that undoes it are live. Below 480px the kind takes its own line, deliberately (D197). **D188 is untouched**: still two halves per kind, either settable first, a half-made trade still a decision in progress and never an error. Measured on a real Bard: **313px open, 291px closed**, row label 0.00px off its chips, opener 34.00px with 8/8 and 349.59/349.59 padding, nothing clipped at 375. |
| **1.6.8** | — | — | **The newest printing wins, and only among the books you have on** (**D203**, his bugs: *"underdark 2 only shows 4 species rather than 5"*, and *"if a later book is not included in the filter selection, the entity from older books should appear"*). The file ships five species and both extractors emitted all five — the picker hid one. `collapseEditions` ranked printings from a hand-kept table of **six** core codes that scored every other book 10, so the **2014 DMG** outranked a 2026 UA book and swallowed its Kuo-Toa silently. Three changes: ① the rank is now the book's **own publication date**, which both extractors carry as `sources[code].released` (5etools `published`, a brew `dateReleased`; 65/65 books have one) — his call, with its cost stated: a book published after the 2024 core outranks it for a name they share. ② The contest is between the books that are **ON**, re-run on every source change — turning the newer book off used to hide the older one too, shadowed by a winner not even in play. ③ Nothing is hidden without evidence: a flagged reprint, or two known dates. An undated book is unknown, not old (D31) — measured at zero visible duplicates across the real 44-book library. **Engine fixture 23** pins all three, and fails on the old table. Still open (⚑ PLAN): a command on an entity to see and switch to an older printing. |
| **1.6.7** | — | — | **The import tray's top row sits on one baseline** (**D202(e)**, his note on v1.6.6: *"the spacing with the top row"*). The heading carries a 15px serif title beside an 11.5px outcome line, and `align-items:center` aligns two different line-height BOXES rather than their ink — the outcome floated **3.9px above the title's baseline**, which is why measuring the boxes had said the row was fine. `align-items:baseline`, with the icon-only help button keeping `align-self:center` since it has no baseline to share. Measured with a zero-height strut in each: **0.00px**. |
| **1.6.6** | — | — | **The import panel says what happened, and Unearthed Arcana has a catalogue** (**D202**, his bug: *"this panel isn't very clear and isn't clear when it's resolved… I don't understand why it doesn't add a new source (Underdark Options 2)"*). ① **One outcome sentence** replaces "Nothing is stored yet" — `59 files · 44 books · none of them new`, in the accent only when something is new. The panel no longer says "nothing new" twice 400px apart, the 260px empty acre is gone, and **Re-read loses the accent**: a re-read is maintenance, not the point of the panel. ② **Applying leaves a RECEIPT.** It used to empty the tray and hide it, so a fading toast was the only word anything had happened. The tray stays — what arrived, by name, what the library now holds, and a Close that is the only thing that dismisses it. ③ **It says which shelf it read**, and — on the 5etools fetch with nothing new — that Unearthed Arcana and homebrew are **not in that repository**. Which was the whole answer: "Update data" reads `5etools-mirror-3/5etools-src` `data/**`, and that tree contains no UA source at all. ④ **So UA is a second source, fetched as a catalogue.** Its repository has no releases (there is no version to pin) and holds 105 books of playtest material, so instead of downloading it, one 8 KB index names every book, the tray lists the ones you don't have as available, and **only a ticked book's file is downloaded** — D112's own mechanism, the one the folder scan already uses, so staging, the plan, the keep-set and Apply are unchanged. Actions → Unearthed Arcana catalogue. |
| **1.6.5** | — | — | **The mirror moved, and the data caught up.** The local 5etools mirror is `5etools-src-main` (**v2.35.1**) and the old `5etools-v2.33.3` path it was read from is gone — `extract.py`, `cparity.js` and `jsimport.js` all defaulted to a directory that no longer exists, so the parity gate could not run at all. Repointed, re-extracted and rebuilt; the public SRD bundle moves with it. One real content change came with the newer mirror: **Warrior of the Mystic Arts** (Arcana Unleashed) is a 1/3-caster Monk subclass casting from the Sorcerer XPHB list, so `cparity`'s pinned caster-subclass census goes 6 → 7, checked record by record as its comment requires. No extractor logic changed; parity is **0 fail**. |
| **1.6.4** | — | — | **The class picker opens again** (**D201**, his bug: *"on guided builder, when selecting a class, the picker shows nothing and seems to refer to backgrounds instead"*). One crash reading as two bugs. `ability` is an **array of raise-groups on a feat** and the **spellcasting score — the bare string `"int"` — on a class**; the picker's ability-bonus filter always read it, harmlessly, because `!ENT.raise.size` short-circuited while no score was ticked. **D199(i) made that axis a three-way**, and `triOk(state,has)` evaluates its `has` as an argument *before* deciding the axis is resting — so every class row threw, on every open, in the guided builder's class step and in "Change the class at level N" alike, since v1.6.1. And because `renderEntityList` empties its list on the first line and writes its count two thirds down, the dead list sat under **the previous picker's count**, which is why a Class step read "70 backgrounds". The fix is the rule rather than a type guard: the row filters now run only where `entFilterGroups` offers them (`entRowOk` — no Spellcasting or Prerequisites for a class, the ability strip for a feat alone), so the menu and the predicate are one list instead of two that drift. **Engine fixture 22** pins it. |
| **1.6.3** | — | — | **The theme is a switch and the palette is one row** (**D200(g)**, his two notes on v1.6.2). "Toggle theme" becomes a **Dark theme** switch beside Full character, reading the system until switched; the six palette swatches fold into a single row that shows the current one flush right and opens the six beneath it on click, closing on a pick and whenever the ⋯ menu opens. |
| **1.6.2** | — | — | **The palette is Amethyst, and the palette is a preference** (**D200**, his ask: variations on the palette from @color.bears). Two mockup rounds on the real app — 30 candidates auto-mapped from 33 scraped palettes, then his shortlist of seven — and his pick: **neutral grey paper, an amethyst accent, the swap violet moved to teal, a real ochre gold.** The rest of the shortlist ships as **variants in the settings menu** beside the theme — Ember (the D124 palette, as it was), Velvet, Sky, Petal and Cinder — six 22px swatches, remembered in localStorage like the creation mode and applied by a head script before the first paint. Every ink of every palette is DERIVED at the Ember palette's measured steps (panel 1.20/1.09, line 2.22/1.33, accent 6.5:1 as text), dark surfaces derive desaturated, and good/swap/alert are chosen per palette for hue distance — alerts always red. Print keeps its paper and takes the main accent. Open from the round: the light modes are papery and short of colour, a design-system task (⚑ in PLAN). |
| **1.6.1** | — | — | **A filter can say NO, a sort can be undone, and every switch in the app is visible again** (**D199(h–j)**, his four notes on v1.6.0). ① **The switch's ON state has been invisible app-wide** — `.menupop button` sets `background:none` and outranks a bare `.swk`, so every switch inside a popover drew its "on" at full transparency while its "off" looked perfect; the filter menus since M2 and the creature carousel's Marked since D81. One rule fixes all of them, and a **disabled** switch now looks disabled rather than merely off. ② **An axis reads three ways now, not two.** "Empty means all" could say ANY and YES and never NO — so Ritual, Concentration and the entity picker's Spellcasting take a **Any · Yes · No** strip instead of a switch, and **Save, Damage, Condition** and the feat picker's **Ability bonus** gain the same strip above their values: *"which of my spells force no save at all"* is finally askable. The strip is the control and its segments are parts of it, the way the tabs already work (D198(d)). The Build tab's own panel keeps its selects and takes **Any** and **None** as options there instead. ③ **The sort is a chip.** It joins the same row as the filters — in the panel's tone rather than the accent, since it states an order and not a narrowing — carries its caret, and drops with its own ×; the field's clear-all now empties the row it sits in, sort included. Engine fixture 21 pins the three-way rule, the migration of a stored boolean, and the trap where a value ticked under "No" quietly widens the axis back open. |
| **1.6.0** | — | — | **The spell table filters and sorts** (**D199**, his ask, designed against four mockups). One filter button joins the card heading and opens a sectioned popover holding twelve axes: the four only this table can ask — **how a spell is prepared, who grants it, which ability casts it, which book it is printed in** — and then the picker's own set, reused verbatim from D174, so "empty means all" and OR-within/AND-across mean exactly what they mean everywhere else. What you have set reads as a **chip field above the table**, there only when it is narrowed, and **every chip drops its own axis** with a clear-all beside them; the button wears the accent while anything is on. **Sorting is both a gesture and a state**: click a column header to sort by it, click again to reverse, and the ⋯ menu names the same sort beside Group by — which gains **No grouping**, the way to read the whole list as one ordered run. A sort orders rows *inside* the groups it does not dissolve, and level-then-name is still the tie-break under all of it. Filter, sort and grouping persist beside the columns, so the reading you set up survives a reload. **Paper ignores the filter unless you say otherwise** — a new print option, off by default, because a sheet quietly carrying half a spell list is worse than no filter at all. Two defects fell out of building it: the chevron `filterMenu` draws on every filter group has been **0×0 and invisible since M1b** (`.lvlcar` was only ever dressed under three other ancestors), and `.afchip` stood 0.5px off the chip token (D198). Engine fixture 20 pins that a filter is a lens — it changes what the table returns and never what the build stores. |
| **1.5.56** | — | — | **One control scale, applied across the app** (**D198**, his call). A census of every button the app renders — nine surfaces, 40 class signatures — found **nine different heights** for things that read as buttons, and four more for chips; the same class even changed size by context, so the filter funnel was 27px in a card and 34px in the header. There are three sizes now and “bare”: **34px** for anything you click or type in a form or toolbar, **28px** for dense contexts like a modal's closer, **22px** for a tag you toggle, and no box at all for a glyph icon. Fields, pickers, buttons, the level stepper and the icon buttons are one height; the chips are another. List rows, the tab strip and the phone nav keep their own sizing on purpose, and so do the parts inside a control. Two sweeps hold it: nothing is clipped anywhere, and no row holds two controls more than 1.5px apart — both empty at 375 and 1280. |
| **1.5.55** | — | — | **Prepare daily is the same size as the button beside it** (**D196(f)**, his follow-up). Centring its icon in v1.5.53 made the real mismatch visible: the button was 28px tall against the ⋯ menu's 34px in the same header, because it took the generic button padding while its neighbour takes the header control height. Both modes are 34px now, and the icon-only one takes the ⋯'s own inset so the two read as one pair. Same height and same top at 320, 420, 620, 621 and 1280. |
| **1.5.54** | — | — | **The class row restacks on a phone instead of squeezing its selects** (**D197**, his ask, off D196's measuring). The four-column row gave the CLASS field 64px at 320px — 36px of which is the select's own padding and caret, so “Wizard” read `Wiza` under the arrow; at 375px there were 50px, enough for the shortest class name and no subclass. Below 480px the row is two lines now: the class on its own with the ✕ at its right, the subclass and Lvl beneath. 26px of text room becomes 177px at 320, and 50px becomes 232px at 375. Above 480 the row is unchanged. |
| **1.5.53** | — | — | **The level chip moves to the app header** (**D196**, his ask). “L7 / 20”, its ⚠ from the build-health sweep and the only way into the timeline used to live in the Character card's heading, which the Spell table view does not draw — so on the surface that shows exactly those spells, all three were gone. They sit in the header now, in line with the character selector, and on a phone the two share a full second row with the chip at its right edge, flush with the ⋯ menu above it. The header holds to one rule from now on: **one row, or two full ones** — never a ragged wrap. It is `nowrap` above 820px so only the character's name gives way, and the name **ellipsises instead of bleeding**; three separate holes in that chain had it running 209px past the right edge with a horizontal scrollbar behind it. At 320px everything tightens a notch rather than the ⋯ menu wrapping alone. And **Prepare daily's icon is centred on a phone** — a plain block button sat it on the text baseline, 5.25px high. Every other icon-only button in the app was measured against its own box at 375 and 1280, across nine surfaces: that one was the only offender. |
| **1.5.52** | — | — | **A book that is here and turned on is not a gap** (**D195**, his report; D189 one condition along). On the public build a Wizard holding any XPHB feat the SRD subset does not carry drew the bar reading “0 picks need a book you have turned off · Player’s Handbook (2024)” with a **Turn them on** button that could never clear it — the book is loaded, it is on, and the data simply has no record under that key. That is not a book problem, so nothing about it reaches the bar; a pick from a book you really have off or have never loaded reports exactly as before. Found alongside it: a subclass’s uid is `Short|Class|ClassSource|Source`, so the book was being read from the wrong segment and a missing Bladesinging asked you to *re-import “Wizard”* — a book that does not exist. The bar also refuses, on its own account, to render a zero count or to name a book already on. Engine fixtures 17f–17i. |
| **1.5.51** | — | — | **A feat you have not reached yet is not a feat you fail** (**D194**, his report). Previewing a level below where a feat was taken still drew its chip on the Character card — and then judged its "Level 4+" prerequisite against the level you were looking at, so a perfectly legal general feat wore a red "prerequisite not met" warning at level 1 while the budget counter beside it correctly read 0/0. The engine was right all along: every per-level reader already took the slice, and `renderFeatChips` was the last one walking the raw array. It follows the previewed level now, like the picked-spell chips always have. Removing a chip still writes by index into the build, empty slots and all. |
| **1.5.50** | — | — | **Backgrounds, as the origin and nothing else** (**D191**; Phase N's N2). A background is a real entity now: pick one on the Character card or in the guide, and it narrows the origin +2/+1 to the three abilities it names — the +2/+1 vs +1/+1/+1 budget still deciding inside them, and a bonus you already hold always keeping its own pill so you can undo it. It names its origin feat and offers to take it into the origin slot with one click; nothing is ever written to your feats behind your back. Its skills, tools, languages and equipment are printed on its detail under a line saying plainly that the app does not track them — this rung models the origin, not a character sheet. 2024 backgrounds only, since a 2014 one has no ability block and would grant nothing. Both extractors read `backgrounds.json` and parity diffs all 60 records byte for byte; the SRD bundle carries four, so the public build has them without an import. In Simplified creation the whole thing is absent (D192). |
| **1.5.49** | — | — | **Character creation is a mode, and Simplified is the default** (**D192**, his ask; **D193** for the add-class row). A switch in the settings menu: Simplified is the app as it stood before N1 — no ability scores, no origin bonus, and the proficiency bonus, spell save DC and attack back to blanks for a human to fill, which is not a new code path but the fallback a build with no score entered already took. It hides and never prunes: flip to Complete and back and every score and pill is where you left it, the stored build byte-identical. App-wide, not per character. Two gates do all of it — `abilityScores` returns the empty set and `featScoreGains` asks no question — so every derived number and every score choice follows without knowing the mode exists. Engine fixture 18 pins that Simplified derives nothing and that the round trip is lossless. And **Add a class** takes the score popover's dashed full-row Add: centred, muted, with the `+` doing the work the caret was doing, measured symmetric to 0.00px at both widths. |
| **1.5.48** | — | — | **The detail modal's close button works over its own icon** (**D190**, his report). The one closer is delegated and tested the class on `e.target` — but every control here is an icon button, so the click lands on the `<svg>` inside it and the target is never the button. The middle 14px of a 28px control did nothing; the 7px ring around the glyph closed it. The test walks up from the click now (`closest`), which fixes all four bodies that borrow the detail surface (spell, creature, entity, feature) in one line. The check dated from v4, when the `×` was a typed character; the handlers sweep fails on the pattern from now on. |
| **1.5.47** | — | — | **A gap is a pick whose book is missing, and an ability id is not a spell** (**D189**, his live-build report). The bar read "1 pick needs a book that isn't loaded, so re-import it" over a pick called `cha` from a book with no name — unclearable, since no import can satisfy an empty book code. `state.choices` holds three shapes and only one is a list of spells: a SCORE choice stores an array of ability ids, and `buildGaps` walked those as spell keys. It tests the key now (`name|source`), and a reference naming no book is never reported at all. A real pick from a book you have off or have not loaded reports exactly as before. |
| **1.5.46** | — | — | **The level-up trade is two halves, and neither binds the other** (**D188**, his shape off the `swap1` mockup). "Giving up" and "Learning instead" are two rows of the trade section, set independently and in either order. The spell you give up is a real chip, so clicking its name opens the detail like every other spell in the build; the replacement is an ordinary spell choice beside it. A half-made trade is a decision in progress, not an error — the card says which way it is short and nothing goes red. The slot a give-up vacates is tagged, so no ordinary take can land in it and the level card draws it as "Traded away" rather than "Empty slot". And the rail carries a trade only once one exists, half-made included: sixteen `to decide` rows gone on a Warlock 9. Old two-part events are the same shape with both halves present, so nothing stored needs migrating. |
| **1.5.45** | — | — | **A choice that came with something folds into it, and an import supersedes the licensed-name twin** (**D187**, two more of his six). The rail said the same decision twice — `Eldritch Invocations · Agonizing Blast`, then the whole prompt sentence with `Eldritch Blast` under it. A single choice that is forced or already answered now folds into the answer that granted it: `Agonizing Blast (Eldritch Blast)`, one row, card and rail alike, with the suffix as the way back. A DEFAULT never counts as an answer and a group asking for several never folds. Separately, SRD 5.2 strips the wizard's name off 17 spells and the public bundle is built under the licensed ones, so an import carrying "Tasha's Hideous Laughter" left the baked "Hideous Laughter" standing beside it. The import supersedes its twin now, matched by the SRD alias the imported record carries, so nothing licensed is ever baked into `docs/` and a baked grant naming the licensed spell still resolves. |
| **1.5.44** | — | — | **A pick you already hold never moves, and a granted spell says so** (**D186**, two of the six items in his report). Standing at level 3 and clicking a spell the build acquires at level 8 used to re-date five picks and could drop a 4th-level spell into a level-4 slot — `toggle`'s pull-back still spliced, the move D146 outlawed for drops. It refuses now and names the level the pick really arrives at. And the guide's picker marks a spell your build already grants: "always prepared" where a subclass or class feature hands it to you free, "already granted" for a feat, species or custom source, since a limited free cast is still worth taking as a known spell. Nothing is hidden or blocked. |
| **1.5.43** | — | — | **Details reach the character view, and a guide step changes its feat instead of overspending** (**D185**, his two reports). The guide BORROWS the app's single detail surface for its preview pane; sliding aside returned before handing it back, so the walk went off-screen holding it and every detail the character view opened rendered into an inert box nobody could see. It comes back on the way out now, with the picker left hosted so returning finds the step as you left it. And a step that owns one slot changes its answer: clicking a second origin feat read `origin 2/1` with the card still naming the first, so once the slot budget is full a take drops what that step holds and the new entry lands in the slot it leaves, keeping its level. Within budget nothing changes — two metamagics at Sorcerer 2 are still two clicks, and an in-budget add never re-points the step at a sibling's pick. Feats and optional features both; outside the guide the add-and-flag model (D42) is untouched. Engine fixture 14 covers the cap rule. |
| **1.5.42** | — | — | **The card you are standing on owns the slot, and Skip is final** (**D184**, his two-bug report). Dropping one 1st-level pick from a level 5 card used to re-cap that card at 1 — 112 spells to 30, every 2nd- and 3rd-level one gone — because D125 read the ROW's first open slot as the landing for every section of the row, a premise D146's empty slots retired. A section's landing is its OWN first open slot now (`secOpenSlot`, the single owner the cap, the view and the writer all read); the pool is the section's reach; the view stands on the section's own level; and where a full section really will land a pick in an earlier empty slot the hint says so per level instead of the list shrinking. Skipped levels leave empty slots behind, so a take on the L3 card lands at the L3 slot rather than falling into slot 0 and going red. And **Skip is final**: the walk records a step it moves off while still open and may never clamp you back onto it, which is what had Skip alternating between two steps forever. Engine fixture 13 covers both rules. |
| **1.5.34 → 1.5.41** | — | — | → archived 2026-09-07: 8 rows — Phase N's N1 (ability scores and the proficiency bonus, then four review rounds) and the `classVariant` extractor gap — `ARCHIVE.md#changelog-1534-1541` |
| **1.5.0 → 1.5.33** | — | — | → archived 2026-09-07: 34 rows, the Library redesign (K1–K4), the audit build list (L5.1–L5.4) and the filter system's first half — `ARCHIVE.md#changelog-150-1533` |
| **1.0.0 → 1.4.14** | 1.0 → 1.8 | — | → archived 2026-09-05: 68 rows, the builds/timeline/guided-builder line — `ARCHIVE.md#changelog-10-14` |

→ archived 2026-08-31: the full release narrative for **v1.2.0 → v1.2.41** (phases E–I and
the wave batch) — `ARCHIVE.md#changelog-12x`. The rows above keep the tag map.

## 0.x — before the numbering (tagged retroactively)

| Tag | Commit | What shipped |
|---|---|---|
| `v0.4`   | `5762b2e` | Repo split, choices engine, casting stats, spell table + details. |
| `v0.6`   | `468436f` | Runtime content layer, custom spells, the 5etools importer, the no-data deploy build. |
| `v0.6.1` | `cbf150d` | SRD-embedded public build, reworked level budget, zip import. |
| `v0.6.2` | `0cc931f` | Per-level caps, edition dedup, the wizard book model, spell-modal access. |
| `v0.6.3` | `58d0cc5` | Grant feature names, quiet description sub-headings, collapsed Access. |
| `v0.6.4` | `575ce5c` | Dedicated species and feat picker modals. |
| `v0.6.5` | `f0ac6b8` | Innate-cast fixes, the shared source checklist, the column rework, optional features. |
| `v0.6.6` | `a4958e2` | Prerequisites end to end, grouped choices, picker menus, quieter components. |
| `v0.7`   | `1115e1f` | Saved builds complete — storage, activation, the manager, the switcher, export/import, IndexedDB. |

Between `v0.7` and `1.0`, unnumbered: the print / save-as-PDF surface, the offline install for
the published build, and feature-granted familiar forms (decisions D97–D109 in `STATE.md`).

## What shipped, by phase

Moved from `STATE.md`'s Shipped list on 2026-08-27 (v1.1) — one owner for "what shipped when".
Per-batch narrative → `ARCHIVE.md#v7-batches`.

- v6 / v6.1 — runtime content layer, custom spells, importer, Pages deploy, SRD embed, zip import.
  → `ARCHIVE.md#v6`
- v6.5 / v6.6 — innate-cast fixes, optional features, prerequisites end-to-end, column rework,
  shared source checklist, unified feat picker. → `ARCHIVE.md#v65`
- v7 · T1–T5 — saved builds end to end: storage + migration, activation reconciliation, the
  manager, the header switcher, and export/import as files.
- v7 · T7 — storage-pressure reporting, closed by moving the imported digest to IndexedDB (D93).
- **Importer rework** (2026-08-27) — **D90–D93**: app icon; honest zip errors + unpack progress +
  the clobbered spell-lookup fix (D91); folder scanning indexed by book (D92); IndexedDB (D93).
- **Custom sources** (2026-08-27) — **D94–D95**: the editor redesigned progressive with a live
  summary and per-spell notes, then its two model gaps closed (payment per spell; a `total` unit
  for uses that never reset). The D55/D65 model was never the problem; D95 widened it.
- **Print / save as PDF** (2026-08-27) — **D97–D103, D106–D108**. The whole printed surface: the
  sheet is the spell table under a build summary, with a tick-box tracker for everything
  expendable, a legend of the marks it uses, an optional full-text spell-card appendix with
  internal links, and a remembered settings modal (colour, orientation, tracker, cards,
  all-preparable, page-per-level, notes) that says what a setting will cost.
- **Offline install** (2026-08-27) — **D99**. `build.py` writes a manifest, a stale-while-revalidate
  service worker and a 192/512/maskable icon set into `docs/` only; registration is guarded on
  `__PUBLIC__`. ⚑ Registration is **unverified** — the in-app browser refuses to register a worker
  and Chrome was unreachable from the session (owner: Francesco, 2026-08-27).
- **Table & data fixes** (2026-08-27) — **D100** (a source states only the numbers a spell rolls),
  **D104** (grouping by source folds a subclass or invocation into its class), **D105** (marked
  summon forms print and lead the carousel), **D109** (a feature can add forms to a familiar
  spell), plus the creature-book filter bug → Gotchas.
- **v7 note batches 1–10** (2026-08-26 → 2026-08-27) — **D43–D89**. Ten batches of notes plus two
  bug hunts on top of the tasks. Per-batch narrative → `ARCHIVE.md#v7-batches`; the earlier notes
  → `ARCHIVE.md#v7-notes`. The load-bearing outcomes are the decisions above and the Gotchas
  below — this list used to restate them a third time.
